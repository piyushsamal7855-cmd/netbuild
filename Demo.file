<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Theos Cafe | Advanced Experience</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

    <style>
        :root {
            --gold: #d4a373;
            --dark: #0f0a08;
            --glass: rgba(255, 255, 255, 0.05);
        }

        body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
            background: #0f0a08;
            color: #f5e6d3;
            overflow-x: hidden;
        }

        /* Animated Background Gradient */
        .bg-animate {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: radial-gradient(circle at 50% 50%, #2c1e14 0%, #0f0a08 100%);
            z-index: -1;
            animation: pulse 10s infinite alternate;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            100% { transform: scale(1.1); }
        }

        /* Hero Section with Parallax */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            background: linear-gradient(to bottom, rgba(0,0,0,0.3), #0f0a08), 
                        url('https://images.unsplash.com/photo-1551024601-bec78aea704b?q=80&w=1964&auto=format&fit=crop') center/cover;
            padding: 20px;
        }

        .hero h1 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(3rem, 10vw, 6rem);
            margin: 0;
            color: var(--gold);
            text-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        /* Glassmorphism Cards */
        .glass-card {
            background: var(--glass);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(212, 163, 115, 0.2);
            border-radius: 20px;
            padding: 30px;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .glass-card:hover {
            border-color: var(--gold);
            transform: translateY(-15px) scale(1.02);
            box-shadow: 0 20px 40px rgba(0,0,0,0.6);
        }

        /* Floating Animation for Buttons */
        .btn-premium {
            padding: 15px 40px;
            background: var(--gold);
            color: #000;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 2px;
            display: inline-block;
            margin-top: 30px;
            box-shadow: 0 0 20px rgba(212, 163, 115, 0.4);
            transition: 0.3s;
        }

        .btn-premium:hover {
            box-shadow: 0 0 40px rgba(212, 163, 115, 0.8);
            letter-spacing: 4px;
        }

        /* Grid Layout */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            padding: 50px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-header {
            text-align: center;
            padding-top: 100px;
            font-size: 2.5rem;
            font-family: 'Playfair Display', serif;
        }

    </style>
</head>
<body>

    <div class="bg-animate"></div>

    <section class="hero">
        <div id="hero-content">
            <p style="letter-spacing: 5px; opacity: 0.8;">SURAT'S PREMIUM</p>
            <h1>Theos Cafe</h1>
            <p style="font-size: 1.2rem;">Brownies • Waffles • Coffee • Cakes</p>
            <a href="#" class="btn-premium">View Menu</a>
        </div>
    </section>

    <h2 class="section-header">Our Signature Treats</h2>

    <div class="grid-container">
        <div class="glass-card reveal">
            <h3>Biscoff Brownie</h3>
            <p>Served warm with premium vanilla bean ice cream and melted biscoff drizzle.</p>
            <span style="color:var(--gold); font-weight:bold;">₹249</span>
        </div>
        <div class="glass-card reveal">
            <h3>Nutella Waffle</h3>
            <p>Golden crispy waffles loaded with authentic Nutella and roasted hazelnuts.</p>
            <span style="color:var(--gold); font-weight:bold;">₹299</span>
        </div>
        <div class="glass-card reveal">
            <h3>Signature Coffee</h3>
            <p>Freshly brewed Arabica beans with a touch of dark chocolate.</p>
            <span style="color:var(--gold); font-weight:bold;">₹189</span>
        </div>
    </div>

    <script>
        // GSAP Animations
        gsap.registerPlugin(ScrollTrigger);

        // Hero Text Animation
        gsap.from("#hero-content > *", {
            duration: 1.5,
            y: 50,
            opacity: 0,
            stagger: 0.3,
            ease: "power4.out"
        });

        // Scroll Reveal Animation for Cards
        gsap.utils.toArray(".reveal").forEach(card => {
            gsap.from(card, {
                scrollTrigger: {
                    trigger: card,
                    start: "top 85%",
                },
                duration: 1,
                y: 100,
                opacity: 0,
                ease: "power2.out"
            });
        });
    </script>

</body>
</html>
